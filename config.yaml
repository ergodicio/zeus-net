
fitter:
  learning_rate: 0.001

data:
  crop: 256
  x_downsample: 20 # nx = (2160 - 512) / x_downsample
  y_downsample: 50 # ny = 3840 / y_downsample
  batch_size: 32


model:
  num_channels: 16
  num_layers: 4
  log_loss: False

mlflow:
  experiment: zeus
  run: zeusnet